# mywoods CMS server

Simple Express API for CMS CRUD.

## Endpoints
- GET    `/api/woods`
- POST   `/api/woods`
- PUT    `/api/woods/:id`
- DELETE `/api/woods/:id`

Data is stored in-memory (restart pe reset).

## Run locally
```bash
npm install
npm run dev
```
Server: http://localhost:5000

